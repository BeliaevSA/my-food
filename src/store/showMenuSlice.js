import { createSlice } from "@reduxjs/toolkit";

const showMenuSlice = createSlice({
  name: 'showMenu',
  initialState: {
    breakfast: 0,
    snack: 0,
    lunch: 0,
    supper: 0,
    adding: 0,
    all: 0
  },
  reducers: {
    
  }
})